#profundidade
list, dict, set, tuple — diferenças, performance e usos ideais
enumerate(), zip(), any(), all()
Compreensões: listas, dicionários e conjuntos
*args e **kwargs
# Funções e estrutura
Funções como objetos (passar funções como parâmetro)
Funções lambda
Closures
Decoradores (@decorator)
#🧱 Programação Orientada a Objetos
Métodos mágicos (_init_, _str_, _repr_, _eq_, etc.)
Herança múltipla
super()
Classes abstratas (abc)
@property, getters e setters
# Módulos e pacotes
_name_ == "_main_"
Ambientes virtuais (venv, poetry, pipenv)
Estrutura de projetos Python
_init_.py
# Tratamento de erros
try/except/else/finally
Criar exceções personalizadas
raise
# Iteradores e geradores
iter(), next()
yield
Geradores com expressão: (x for x in range(10))
# Concorrência e paralelismo
threading
multiprocessing
concurrent.futures
# Assíncrono (async/await) — muito importante hoje!
async def, await
asyncio.run()
asyncio.gather()
Diferença entre assíncrono, threads e processos
Quando usar async (I/O: API, banco, arquivos, web)
# Testes
unittest
pytest
Mock (unittest.mock)
# Tipagem e qualidade de código
Type hints (def func(x: int) -> str:)
mypy
dataclasses
pydantic
# Arquivos e sistemas
with open(...)
pathlib
os, shutil
# Depuração e performance
print() estratégico 😅
pdb
timeit
Profiling (cProfile)